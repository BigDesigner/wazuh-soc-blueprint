# rules/
Place custom Wazuh rules here.

Recommended:
- Keep rules modular by detection domain (auth, rdp, privesc, persistence, ti)
- Map rules to MITRE ATT&CK when possible
- Maintain a `testing/` subfolder for staging rules before production rollout
